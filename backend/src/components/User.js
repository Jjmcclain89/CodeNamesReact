export default class User {
    constructor({ id, client, userName }) {
        this.id = id;
        this.client = client;
        this.userName = userName;
        this.team;
        this.isClueGiver;
    }
}


